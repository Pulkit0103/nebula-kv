# Build output
target/

# IDE
.idea/
*.iml
.vscode/
*.code-workspace

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Environment / secrets
.env
*.env
*.env.*
!*.env.example

# JVM crash logs
hs_err_pid*.log
replay_pid*.log

# Maven wrapper (optional — uncomment if not using wrapper)
# .mvn/wrapper/maven-wrapper.jar

# Runtime data directories (database files — never commit)
# Anchored to root so they don't accidentally match source packages like src/.../wal/
/data/
/wal/
/sstables/
/snapshots/
